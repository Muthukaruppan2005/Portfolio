{
  "name": "my-portfolio-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "mongoose": "^8.17.1"
  }
}
